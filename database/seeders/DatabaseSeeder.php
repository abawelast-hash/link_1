<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * بذر قاعدة بيانات التطبيق.
     *
     * البيانات الأولية تُنشأ عبر:
     *   php artisan sarh:install
     *
     * هذا المُبذِّر يُشغِّل البذور الأساسية فقط.
     */
    public function run(): void
    {
        // لا توجد بذور تلقائية — استخدم php artisan sarh:install
        // لإنشاء: الفروع، المدير العام، الشارات، إعدادات المنافسة
    }
}
